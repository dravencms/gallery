{block #content}
    <p n:isAllowed="gallery, edit">
        <a n:href="Gallery:editPicture galleryId => $gallery->getId()" class="btn btn-primary"><i class="fa fa-plus"></i> {_"New photo"}</a>
        <a n:href="Gallery:newPictureFromDirectory galleryId => $gallery->getId()" class="btn btn-primary"><i class="fa fa-plus"></i> {_"New photos from directory"}</a>
    </p>
    {control gridPicture}
{/block}
